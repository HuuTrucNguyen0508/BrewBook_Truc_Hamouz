apiVersion: v1
kind: Service
metadata:
  name: {{ include "brewbook.fullname" . }}
  labels:
    {{- include "brewbook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "brewbook.selectorLabels" . | nindent 4 }}
